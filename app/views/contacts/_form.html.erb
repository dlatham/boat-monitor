<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :bilge_main %>
    <%= form.check_box :bilge_main %>
  </div>

  <div class="field">
    <%= form.label :bilge_backup %>
    <%= form.check_box :bilge_backup %>
  </div>

  <div class="field">
    <%= form.label :charge_error %>
    <%= form.check_box :charge_error %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
