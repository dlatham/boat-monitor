<%= form_with(model: bilge, local: true) do |form| %>
  <% if bilge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bilge.errors.count, "error") %> prohibited this bilge from being saved:</h2>

      <ul>
      <% bilge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
